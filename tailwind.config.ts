
import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        mse: {
          dark: "#000000",
          purple: "#4B0082",
          magenta: "#E4007F",
          green: "#00A651",
        },
      },
    },
  },
};

export default config;
